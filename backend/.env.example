# Database - Railway PostgreSQL (replace with your actual Railway connection string)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_RAILWAY_DOMAIN:PORT/railway"

# Server Configuration
NODE_ENV="development"
PORT="4000"

# CORS
FRONTEND_URL="http://localhost:3000"

# Future: OpenAI Integration
# OPENAI_API_KEY="your-openai-api-key"

# Production Environment Variables (Railway will provide these automatically)
# DATABASE_URL=<railway-postgresql-internal-url>
# NODE_ENV=production
# PORT=<railway-assigned-port>
# FRONTEND_URL=https://your-username.github.io/food-tracking

# Railway deployment notes:
# - DATABASE_URL will be automatically provided by Railway PostgreSQL service
# - PORT will be automatically assigned by Railway (usually from $PORT env var)
# - Set FRONTEND_URL to your GitHub Pages URL for CORS
# - NODE_ENV should be set to "production" in Railway environment