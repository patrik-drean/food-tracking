# Database - Railway PostgreSQL (replace with your actual Railway connection string)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@YOUR_RAILWAY_DOMAIN:PORT/railway"

# Server Configuration
NODE_ENV="development"
PORT="4000"

# CORS
FRONTEND_URL="http://localhost:3000"

# Future: OpenAI Integration
# OPENAI_API_KEY="your-openai-api-key"

# Production (Railway will provide these automatically)
# DATABASE_URL=<railway-postgresql-url>
# NODE_ENV=production
# PORT=<railway-assigned-port>
# FRONTEND_URL=https://username.github.io/food-tracking